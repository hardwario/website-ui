---
"@hubpav/hwio-ui": minor
---

HWioHeader CTAs accept an `icon` and collapse to the icon alone below xl (label becomes the aria-label; the store button rule). HWioPricingTiers tiers take an `eyebrow`. HWioLogoStrip logos take a `size` step (xs to xl).
