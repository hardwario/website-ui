---
"@hubpav/hwio-ui": patch
"@hubpav/hwio-brand": patch
---

`HWioHeader`: the current page's nav item is marked with a coloured label and a 2px bar on the header's bottom edge (`--hwio-nav-active`, default the primary colour; the `er3o` theme uses ENEROOO's blue `#296ea7`). Registry: the HARDWARIO LinkedIn link is `https://www.linkedin.com/company/hardwario/`.
