---
"@hubpav/hwio-ui": patch
---

Theme `er3o`: the filled primary button dims on hover (`brightness(0.92)`); DaisyUI's hover only changes `background-color`, which the CTA gradient image covered, so the button showed no hover state.
