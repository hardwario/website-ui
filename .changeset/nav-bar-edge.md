---
"@hubpav/hwio-ui": patch
---

`HWioHeader`: the current-page bar now sits on the header's bottom edge (nav items span the bar's height) and the current label colour wins over the default link colour.
