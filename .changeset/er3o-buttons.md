---
"@hubpav/hwio-ui": patch
---

Theme `er3o`: the CTA gradient applies only to the filled `btn-primary`; outline, ghost and link variants keep a transparent background (the consent banner's "Odmítnout" was gradient-on-gradient), and the outline button uses the AA teal accent for text and border. `HWioCookieConsent`: the policy link is `link-accent` (AA on every theme; `link-secondary` was the 2.0:1 green on enerooo).
