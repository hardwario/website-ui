---
"@hubpav/hwio-ui": patch
---

HWioHeader: one language control everywhere (the desktop dropdown; pills only inside the drawer). The `languageVariant` prop added in 0.4.0 is removed (owner rule: keep component variations to a minimum).
